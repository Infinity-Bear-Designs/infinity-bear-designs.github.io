{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start/Update Site",
            "type": "shell",
            "command": "docker-compose up --build -d"
        },
        {
            "label": "Stop Site",
            "type": "shell",
            "command": "docker-compose down"
        }
    ]
}